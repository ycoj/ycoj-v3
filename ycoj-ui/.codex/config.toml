# Headless Chromium cannot launch under the codex workspace sandbox, and
# `pnpm test:browser` needs it; keep full access so agents can run tests.
approval_policy = "never"
sandbox_mode = "danger-full-access"
